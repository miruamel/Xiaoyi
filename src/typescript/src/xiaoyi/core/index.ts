/**
 * # Core Module
 *
 * `core` provides configuration, error handling, and result types.
 *
 * Path: `xiaoyi.core`
 *
 * - Layer 0: `core`
 * - Layer 1: `config` — configuration management.
 * - Layer 2: `error` — error types and handling.
 * - Layer 3: `result` — result/status types.
 *
 * @module core
 * @brief Core infrastructure: config, error, result
 * @group Core
 * @since 0.1.0
 * @author Miruamel
 * @see core.config
 * @see core.error
 * @see core.result
 */
export * from "./config";
export * from "./error";
export * from "./result";